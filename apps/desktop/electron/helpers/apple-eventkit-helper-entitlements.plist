<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
  <!-- Hardened runtime blocks EventKit calendar access (silent deny, no
       prompt) unless the signed binary carries these entitlements. The main
       app has them in release-entitlements.plist; the helper needs its own. -->
  <key>com.apple.security.personal-information.calendars</key>
  <true/>
  <key>com.apple.security.personal-information.reminders</key>
  <true/>
</dict>
</plist>
